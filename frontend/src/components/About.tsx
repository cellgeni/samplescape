import React from 'react';
///import { Breadcrumb } from 'antd';

const About: React.FC = () => {
    return (
        <div style={{ padding: '16px' }}>
            <h1>About</h1>
            <p>search for study and samples from sequencescape and mlwarehouse.</p>
        </div>
    );
};

export default About;